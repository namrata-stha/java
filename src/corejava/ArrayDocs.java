package corejava;

public class ArrayDocs {
           /*
            * =============Array=============
            *  # collection of homogeneous data.
            *  # collection of multiple values of same data type.
            *  # array has its fixed size.
            *  # array contains duplicate data.
            *  # data store on the basis of indices and index starts from 0.
            *  # random access
            *  contiguous memory allocation.
            *      
            *     Syntax:
            *     
            *     data_type_array_name[] = new data_type[size];
            */
}
